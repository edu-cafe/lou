CC = gcc
CFLAGS = -O2 -Wall -g
SRCS = test1.c test2.c test3.c
OBJS = $(SRCS:.c=.o)
#SUFFIXES = .c .o

test: $(OBJS)
	$(CC) -o $@ $^

#.c.o :
#	$(CC) -c $(CFLAGS) $<

clean:
	rm $(OBJS) test

dep:
	gccmakedep $(SRCS)
